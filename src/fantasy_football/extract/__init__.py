"""Extraction layer.

Modules in this subpackage are responsible only for *pulling raw data* from
external sources and caching it locally. Transformation / cleaning logic is
kept separate (see the project's extraction-vs-transformation principle).
"""
